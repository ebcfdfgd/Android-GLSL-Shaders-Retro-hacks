3.14159
#pragma parameter MASK_STR "Sony RGB Mask Intensity" 0.15 0.0 0.5 0.02

#ifdef PARAMETER_UNIFORM
uniform float MASK_STR;
#endif

    if (MASK_STR > 0.0) {
        float pos_x = vTexCoord.x * TextureSize.x *  6.28318;
        
        vec3 mask_rgb = vec3(sin(pos_x), sin(pos_x + 2.0944), sin(pos_x + 4.1887));

        res.rgb += mask_rgb * MASK_STR;
    }
....................................................................................
#pragma parameter MASK_STR "Sony RGB Mask Intensity" 0.15 0.0 0.5 0.02

#ifdef PARAMETER_UNIFORM
uniform float MASK_STR;
#endif

    if (MASK_STR > 0.0) {
        float pos_x = gl_FragCoord.x *  6.28318;
        
        vec3 mask_rgb = vec3(sin(pos_x), sin(pos_x + 2.0944), sin(pos_x + 4.1887));

        res.rgb += mask_rgb * MASK_STR;
    }
.............................................................................................................................
