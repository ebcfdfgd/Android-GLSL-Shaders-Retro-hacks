/* =========================
   CONSUMER CRT TV PROFILES
   (household / VHS / living room feel)
   ========================= */

/* --- 1. Generic Home TV (clean baseline CRT) --- */
const mat3 YIQ_HOME_TV = mat3(
    1.02, 1.00, 1.01,
    0.94, -0.28, -1.08,
    0.62, -0.64,  1.68
);

/* --- 2. Toshiba / Sharp Living Room CRT (semi-flat warm) --- */
const mat3 YIQ_TOSHIBA_HOME = mat3(
    1.04, 1.01, 1.00,
    0.95, -0.26, -1.05,
    0.64, -0.66,  1.62
);

/* --- 3. VHS / RF Cable TV (slightly washed, noisy analog feel) --- */
const mat3 YIQ_RF_VHS = mat3(
    0.98, 1.00, 1.03,
    0.90, -0.30, -1.00,
    0.58, -0.60,  1.55
);

/* --- 4. Japanese Family TV (anime living room look) --- */
const mat3 YIQ_JP_HOME = mat3(
    1.06, 1.02, 0.98,
    0.92, -0.25, -1.12,
    0.60, -0.62,  1.75
);

/* --- 5. Late 90s Living Room CRT (sharp but warm consumer tube) --- */
const mat3 YIQ_90S_HOME = mat3(
    1.03, 1.00, 1.02,
    0.93, -0.27, -1.10,
    0.61, -0.63,  1.70
);

/* --- 6. Cheap Bedroom CRT (soft, slightly crushed blacks) --- */
const mat3 YIQ_CHEAP_TV = mat3(
    0.97, 0.99, 1.01,
    0.88, -0.31, -0.98,
    0.55, -0.58,  1.50
);

/* --- 7. Toshiba Semi-Flat Signature Look (your target) --- */
const mat3 YIQ_TOSHIBA_FLAT = mat3(
    1.05, 1.01, 1.00,
    0.96, -0.27, -1.06,
    0.65, -0.67,  1.63
);