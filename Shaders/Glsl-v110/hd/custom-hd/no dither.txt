vec3 res = c;
................
/* ---------------- SPECULAR (معدل ليفصل عن البلوم) ---------------- */
    // نعتمد على الـ curvature وليس فقط السطوع ليظهر كـ "لمعة" على الحواف
    float spec = curvature * SPECULAR * 0.5; 
    res += vec3(spec);
.................................
/* ---------------- SHARPEN & SHINE (Curvature + Specular) ---------------- */
// نضرب في (1.0 + y_m) لربط اللمعة بالسطوع، وبدونها ستكون مجرد حدة (Sharpening) عادية
res += ((edge + dist) * 0.5) * (1.0 + y_m) * CURVATURE_STR * 0.1;